<h1>Bienvenido a BD-CHAN</h1>
<a href="<%= home_path %>">Ir al Home</a>

<table border="1">
  <thead>
    <tr>
      <th>Secciones</th>
      <th>Fecha Creacion</th>
    </tr>
  </thead>
  <tbody>
    <% @seccions.each do |sec| %>
      <tr>
        <td><%= sec.nombre %></td>
        <td><%= sec.created_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>